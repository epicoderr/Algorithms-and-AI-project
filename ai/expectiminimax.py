from game.board import Board

def expectiminimax(board, depth, is_max_player):

    if is_max_player:
        #So if it is AIs turn, we do something
        return best move
    else:
        #The chance is computed here
        return expected value of the board state